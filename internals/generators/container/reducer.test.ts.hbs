/**
 * Test cases for {{ properCase name}}'s reducers
 * {{> proprietary }}
 */

import { fromJS } from 'immutable';
import {DEFAULT_API} from '../constants';
import {I{{properCase name}}ApiContract} from '../interface';
import {{ camelCase name }}Reducer from '../reducer';

describe('{{ camelCase name }}Reducer', () => {
  it('returns the initial state', () => {
    const initialState: I{{properCase name}}ApiContract = fromJS({});
    expect({{ camelCase name }}Reducer(initialState, {type: DEFAULT_API.REQUEST})).toEqual(fromJS({}));
  });
});
